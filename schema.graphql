enum EventType {
  REWARD
  STAKE
  WITHDRAWAL
}

type LRCEvent @entity {
  id: ID! # hash of transaction in which the event was emitted
  amount: BigInt!
  user: Bytes!
  eventType: EventType!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
}
