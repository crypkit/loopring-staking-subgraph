enum EventType {
  REWARD
  STAKE
  WITHDRAWAL
}

type LRCEvent @entity {
  id: ID! # transaction hash
  amount: BigInt!
  user: String!
  eventType: EventType!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
}
